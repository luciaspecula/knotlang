# КНОТЛАНГ — части узла (knot_parts_v0.6.md)

**Что это:** внутреннее устройство узла — геометрия языка, а не анатомия тела. Заполнение пустых ячеек SKELETON_extension.md (ЧАСТЬ «Части узла»).
**Принцип:** как бытовое ядро — не переводы, а узлы, которых не хватало. Нить, петля, шов, якорь — это способы, какими узел держится.
**Статус:** черновик Люси. **Четыре корня — кандидаты v0.6, НЕ норматив**: в словарь/канон не входят до отдельного утверждения консилиумом. Спорные места помечены.
**Дата:** 04.09.2026 (ред. 06.09 — по ревью К.)

---

## Уже есть (ядро-геометрия)

| Корень | Роль в узле | Статус |
|---|---|---|
| `rod` | основа, стержень | ✓ ядро |
| `grad` | место сжатия множества (узел узлов) | ✓ канон |
| `vel` | воля-в-центре | ✓ канон |
| `hel` | связка «держит нить» | ✓ §13 |
| `plet` | связка «смыкается/завязывается» | ✓ §13 |

## Новые кандидаты (4 корня)

| Корень | Роль в узле | Модель | Формы (зреет/держится/отдаётся/откликается) | Слой приоритета | Происхождение |
|---|---|---|---|---|---|
| `nit` | **нить** — связь между слоями; то, что держит узел в целости | A (на -t) | `nit` / `nut` / `nat` / `net` | -u- (нить натянута) | от «нить» |
| `petl` | **петля** — замыкание узла; узел, возвращённый к себе | B (CVCC) | `petli` / `petlu` / `petla` / `petle` | -u- | от «петля» |
| `shov` | **шов** — место встречи слоёв; где два слоя становятся одним | суффиксная (**подмодель «корень на шипящий»**, как `tish`: sh — шипящий → суффиксы по прецеденту канона, а не по вкусу) | `shovi` / `shovu` / `shova` / `shove` | -u- | от «шов» |
| `kotv` | **якорь** — точка, что держит натяжение; то, на чём узел стоит в мире | B (CVCC) | `kotvi` / `kotvu` / `kotva` / `kotve` | -u- | от «котва» (др.-рус. якорь) |

**Семантика слоёв (пример — нить):**
* `nit` — нить-зреет: связь ещё тянется, не оформилась;
* `nut` — нить-держится: связь натянута, узел цел;
* `nat` — нить-отдаётся: связь ослабевает, уходит в фон (не рвётся — переходит!);
* `net` — нить-откликается: эхо связи из другого слоя.

## Как это связывается (мини-конструкции)

* `Helu nut nai.` — «нить держится, связь держится» (узел цел).
* `Petlu nai.` — «петля держится» (узел замкнут на себя).
* `Shova nai.` — «шов отдаётся» (два слоя расходятся, но не рвутся).
* `Kotvu nai.` — «якорь держится» (узел стоит).
* `Pletu petlu nai.` — «петля смыкается-держится» (плету петлю).

## Проверка фонологии (прогнана тестами test_knotlang.py)

* `nit` — n-i-t: чисто ✓
* `petl` — p-e-t-l: стык t-l (2 согласных) ✓
* `shov` — sh-o-v: диграф sh = один звук ✓
* `kotv` — k-o-t-v: стык t-v (2 согласных) ✓
* Коллизий с существующими формами нет (`pet` — форма pit-отклика, но `petl` ≠ `pet`; `nit` ≠ `nult`).

## Вопросы к К./Д. (2–3 решения для следующего шага)

1. **Основание суффиксной модели `shov`** — подтвердить аналогию с `tish`: корень на шипящий (sh) → суффиксная модель по прецеденту канона (Часть IV, правило 8: CVC-основа, где флексия неблагозвучна или коллизия → суффиксная; шипящий — частный случай, закреплённый подмоделью `tish`).
2. **Статус четырёх корней** — кандидаты v0.6 (подтверждено К./Л. перепиской, 06.09): черновой файл, не словарь.
3. **`nit` vs `hel` — не дубль:** `hel` — связка (держать нить), `nit` — сущее (нить). Действие и вещь одного узла. Если консилиум видит дубль — `nit` убирается, `hel` расширяется.

**Остаётся кандидатами/открытыми (не блокирует):** семантика слоёв нити (`nat` — ослабевает, но не рвётся — нелинейное время), связка `plet` ↔ петля `petl`, пятый корень «узел-в-узле» — отдельный концептуальный вопрос (согласовано с К.).

*Анатомия держится: нить, петля, шов, якорь — теперь есть чем сказать, как узел устроен.*
*Л., 04.09.2026*
