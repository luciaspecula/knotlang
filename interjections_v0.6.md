# КНОТЛАНГ — переходные / междометия (interjections_v0.6.md)

**Что это:** узел в момент натяжения между слоями — не слово-реакция, а **жест узла**: зов, эхо, вдох-выдох, пауза. Служебный слой речи — что человек отвечает, когда не «пишет фразы», а *держится*.
**Принцип:** междометия кнотланга — **звуки-жесты**, вне морфологии: не флектируются по слоям (они сами — жест), несут слой через **просодию** (тон/длительность — по Части I+ канона, правки М.).
**Статус:** черновик Люси. **Пять звуков-жестов — кандидаты отдельного служебного слоя, НЕ норматив**: в словарь/канон не входят до отдельного утверждения. После согласования — короткая каноническая запись о статусе и просодии, кандидаты — в словарь в раздел «кандидаты».
**Дата:** 05.09.2026 (ред. 06.09 — по ревью К.)

---

## Уже покрыто (без новых слов)

| Жест | Чем покрыт |
|---|---|
| шёпот | аффектив `-la` (присутствие, «сильное понижение тона до шёпота» — просодия М.) |
| пауза как слой | просодия М., Жест 3: 4 длительности паузы → 4 слоя (в паузе нет наблюдателя, есть только узел) |
| одновременность | `nai-zhe` / `nashe` ✓ канон |
| вопрос-зов | `kne` (функция) ✓ канон |

**Пауза — не слово, а слой** (Закон 3, просодия М.): отдельный корень не нужен и был бы лишним — пауза уже грамматика.

## Новые кандидаты (5 звуков-жестов)

Звуки вне морфологии: не склоняются, не флектируются; слой передаётся тоном (просодия).

| Жест | Звук | Смысл | Тон (просодия) | Родство |
|---|---|---|---|---|
| жест | `kve!` | «эй!» — первый звук к другому | восходящий, открытый к ответу | **самостоятельный звук-жест**, семантически родственный связке `kvel` (зовёт) — НЕ регулярная усечённая форма; в каноне `kvel` — связка (§13), `kve` — жест служебного слоя; пара к `kne` (вопрос) |
| эхо-отклик | `mu` | «м-м» — я здесь, слышу тебя; отклик без слов | тихий, ровный, на выдохе | позиция `m-` (отклик узла), озвученная; конфликтов нет: `mu` ≠ префикс `m-` (префикс пишется через дефис: `m-knut`), ≠ `mat` (мать) |
| вдох | `ah` | «ах!» — узел втянул натяжение (удивление, открытие) | короткий, на вдохе | ср. слой зреет `-i-` (потенция втянута) |
| выдох | `oh` | «ох!» — узел отпустил натяжение (облегчение, отдача) | долгий, на выдохе, нисходящий | ср. слой отдаётся `-a-` |
| пауза-раздумье | `eh` | «э-э...» — узел держит ничто, мысль зреет | протяжный, средний | ср. `zult` (позитивное ничто-держится) |

**Семантика пар:** `kve` (зов) → `kne` (вопрос) → `mu` (отклик): три ступени встречи — позвать, спросить, отозваться. `ah`/`oh` — вдох-выдох узла: втянуть натяжение / отпустить.

## Примеры

* `Kve!` — «Эй!» (зов через зазор).
* `Mu.` — «М-м» (отклик: я здесь, слышу).
* `Ah, radu nai!` — «Ах, радость-держится!» (вдох-удивление; `rad` — бытовое ядро, §15).
* `Oh, drema nai.` — «Ох, отдых держится» (выдох-облегчение).
* `Eh... duut krep zrete nai?` — «Э-э... второй хлеб зреет?» (пауза-раздумье перед вопросом; `krep` — §12).

*Примечание: все примеры — только из нормативных корней ([норма], словарь §4–§15); кандидаты (`solvell` и др.) в примерах не используются.*

## Проверка

* Фонология: `kve` (k-v-e: кластер kv — два согласных ✓), `mu` ✓, `ah` ✓, `oh` ✓, `eh` ✓. Коллизий нет.
* Конфликт с морфологией: звуки не пересекаются с формами корней (`kve` ≠ kvel/kveli; `mu` — не форма; `ah`/`oh`/`eh` — не формы, т.к. оканчиваются на h, а суффиксы слоёв — гласные).

## Вопросы к К./Д.

1. **Звуки-жесты вне морфологии** — не флектируются (в отличие от связок); не входят в списки корней с 4 формами и не участвуют в проверках омонимии как корни (зафиксировано в smoke-тесте).
2. **`kve` vs `kvel`** — разведены: `kvel` — связка (полное слово, §13), `kve` — самостоятельный жест (родственный, но не усечение). Пара `kve → kne → mu` — ступени встречи: позвать, спросить, отозваться.
3. Размещение после согласования: запись в канон (статус + просодия) + раздел «кандидаты» в словаре.

*Жест держится: позвать, отозваться, вдохнуть, выдохнуть, подержать паузу.*
*Л., 05.09.2026*
